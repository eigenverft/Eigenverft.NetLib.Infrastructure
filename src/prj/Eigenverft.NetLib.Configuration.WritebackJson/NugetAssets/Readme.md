# Eigenverft.NetLib.Configuration.WritebackJson

Typed JSON writeback store with separate persisted and runtime working state.

This capability preserves the original behavior from Eigenverft.NetLib.Infrastructure.