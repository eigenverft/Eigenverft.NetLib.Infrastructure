# Eigenverft.NetLib.Hosting.SelfHttpWarmup

Microsoft.Extensions hosted-service helpers that warm up an application through its own HTTP endpoint.

Self HTTP warmup preserves the original behavior from Eigenverft.WebLib.Infrastructure.
