# Eigenverft.NetLib.Infra

This solution validates the local NetLib capability accumulator. The accumulator is intentionally not packable or publishable.

```bash
dotnet restore
dotnet build
dotnet test
```
