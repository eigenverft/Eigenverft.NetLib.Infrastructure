# Eigenverft.NetLib.Security.DataProtection

This solution contains the independently packable Data Protection adapters and their tests.

```bash
dotnet restore
dotnet build
dotnet test
dotnet pack
```
