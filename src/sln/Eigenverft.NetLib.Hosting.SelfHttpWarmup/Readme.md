# Eigenverft.NetLib.Hosting.SelfHttpWarmup

This solution contains the independently packable self-HTTP warmup capability and its tests.

```bash
dotnet restore
dotnet build
dotnet test
dotnet pack
```
