# Eigenverft.NetLib.Infrastructure

This folder owns the solution for the existing monolithic infrastructure package and its tests.

```text
./                                                     solution directory
../../prj/Eigenverft.NetLib.Infrastructure/             package project
../../prj/Eigenverft.NetLib.Infrastructure.Tests/       test project
```

Open `Eigenverft.NetLib.Infrastructure.slnx` in Visual Studio or run `dotnet` commands from this directory.
